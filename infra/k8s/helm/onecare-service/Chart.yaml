apiVersion: v2
name: onecare-service
description: Opinionated baseline chart for OneCare workloads (Deployments, Services, HPA, PDB, NetworkPolicies, Ingress).
type: application
version: 0.1.0
appVersion: "1.0.0"
