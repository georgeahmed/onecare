AGENTS.md — Access Gate

Scope
- Applies to `apps/access-gate/*`.

Portal Uptime
- Guard core hours; show OOH banner; accept submissions with deferral per config.

Safety Gate
- Transformer-based analysis with timeout + rules fallback; multilingual as configured.

Checklist
- [ ] Core-hours logic uses practice TZ
- [ ] OOH policy respected (accept/deferral message)
- [ ] Safety timeout + fallback implemented

