AGENTS.md — @onecare/statekit

Scope
- Applies to `packages/statekit/*`.

Rules
- Keep minimal: types, base state, machine orchestration. No domain logic.
- Maintain backwards compatibility; version on breaking changes.
- 100% type coverage; add tests for transitions.

