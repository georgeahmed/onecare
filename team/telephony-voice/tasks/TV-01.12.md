Task: TV-01.12 — Observability (correlationId, metrics, spans; PHI-safe logs)

Context
- Provide consistent observability across ingest, ASR, and intent classification without leaking PHI.

Files
- packages/observability/src/logger.ts, src/otel.ts
- apps/telephony/src/application/telephony.state.ts

Steps
1) Ensure `correlationId` is generated/propagated from ingress through to bus publishes.
2) Metrics: counts for `audio.chunks`, `asr.calls.ok/error`, `asr.latency_ms` histogram; intent classification counts; end-to-end processing time.
3) Spans around ingest, ASR, and publish; attach safe attributes only (ids, status). Never log transcript content.
4) Unit tests assert correlationId presence and redaction behavior.

Acceptance Criteria
- Logs/metrics/spans consistent and PHI-safe; tests pass.

Validate
- npm run test; inspect structured logs in a local stub run.

Status Update
- make engineer-done ENGINEER=telephony-voice/engineer-01 TASK='TV-01.12' && make team-status-write

