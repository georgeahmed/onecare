Task: TV-01.5 — Contract-first: telephony schemas + validators

Context
- Enforce schemas-first for telephony events. Generate TS types and compile validators for `call-transcribed` and `intent-classified` to validate at ingress and before publish.

Files
- schemas/telephony/call-transcribed.json (existing)
- schemas/telephony/intent-classified.json (existing)
- packages/events/src/contracts/* (generated)
- scripts/codegen/generate.js (reference)

Steps
1) Review schemas for strictness: `additionalProperties: false`, formats, minimal fields (no PHI beyond transcript where necessary). Update `$id` if changed.
2) Run `npm run codegen` to regenerate TS types; import models in telephony code.
3) Compile validators at startup; validate ASR outputs before building envelopes; validate intent payload before publish.
4) Add contract tests for valid and invalid cases; no network calls.

Acceptance Criteria
- Types/validators current; invalid payloads rejected with safe error mapping; tests pass.

Validate
- npm run codegen && npm run typecheck && npm test

Status Update
- make engineer-done ENGINEER=telephony-voice/engineer-01 TASK='TV-01.5' && make team-status-write

