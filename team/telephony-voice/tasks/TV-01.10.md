Task: TV-01.10 — Idempotency & dedupe (per-call)

Context
- Prevent duplicate publishes and repeated downstream handling by enforcing idempotency per `callId` across the telephony pipeline.

Files
- apps/telephony/src/application/telephony.state.ts
- packages/ports/src/idempotency.ts

Steps
1) Reserve an idempotency key at the start of the pipeline (`callId`) with a reasonable TTL; if already reserved, no-op and return success without duplicate publishes.
2) Use the same key for both `call.transcribed` and `intent.classified` publishes; ensure exactly-once semantics where possible.
3) Emit metrics: `telephony.idempotency.hit/miss`; structured logs include correlationId.

Acceptance Criteria
- Duplicate inputs produce no duplicate events; metrics/logs present; tests cover concurrency.

Validate
- Concurrency tests: multiple identical starts → single publish.

Status Update
- make engineer-done ENGINEER=telephony-voice/engineer-01 TASK='TV-01.10' && make team-status-write

