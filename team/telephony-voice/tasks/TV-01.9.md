Task: TV-01.9 — Intent classification integration + publish intent.classified

Context
- Classify call intent from transcript (stubbed ML ok) and publish `telephony.intent.classified` envelopes with correlationId.

Files
- apps/telephony/src/adapters/intent.client.ts (new)
- schemas/telephony/intent-classified.json (reference)
- packages/events/src/envelope-util.ts, topics.ts

Steps
1) Define `classifyIntent(callId, transcript, lang?) → { intent, confidence }` interface; return stubbed outputs deterministically for tests.
2) Build and validate `IntentClassified` payload; publish via bus with correlationId; ensure idempotency per callId.
3) Metrics: `intent.classify.ok/error`, `intent.confidence` histogram; structured logs with safe fields.

Acceptance Criteria
- Validated payloads; idempotent publish; metrics/logs present; tests pass.

Validate
- npm run test

Status Update
- make engineer-done ENGINEER=telephony-voice/engineer-01 TASK='TV-01.9' && make team-status-write

