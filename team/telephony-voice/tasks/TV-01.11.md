Task: TV-01.11 — Backpressure & rate limits

Context
- Protect the telephony service under load: cap concurrent ASR calls and rate-limit by caller/practice; map overload to safe envelopes and metrics.

Files
- apps/telephony/src/index.ts (ingress)
- apps/telephony/src/application/telephony.state.ts
- packages/config/src/index.ts

Steps
1) Add a semaphore to cap concurrent transcriptions (configurable; default 20–50). On saturation, return 503 `over_capacity` envelope.
2) Implement token-bucket rate limiting per caller/practice with configurable rates; on breach return 429 `rate_limited` with `retry-after`.
3) Metrics: `telephony.queue`, `telephony.reject`, `ratelimit.hit/miss`.

Acceptance Criteria
- Surges controlled; correct error mapping; metrics/logs present.

Validate
- Burst tests; verify controlled reject rate and stable latency.

Status Update
- make engineer-done ENGINEER=telephony-voice/engineer-01 TASK='TV-01.11' && make team-status-write

