Task: MO-01.3 — Add OpenTelemetry middleware/hooks (trace + metrics)

Context
- Instrument Python services with OTEL for traces and metrics (no backend required yet).

Steps
1) Add simple middleware to capture request durations and spans (stub logger).
2) Provide env toggles to enable/disable instrumentation.

Acceptance Criteria
- Logs include trace-like timing; code gated by env.

Validate
- pytest -q services-py/tests (if applicable)

Status Update
- make engineer-done ENGINEER=mlops/engineer-01 TASK='MO-01.3' && make team-status-write

