Task: BE-02.6a — At-least-once semantics + dedupe keys

Context
- Guarantee at-least-once delivery and offer deduplication keys to avoid duplicate processing at consumers.

Files
- packages/bus/src/nats.ts
- packages/bus/src/types.ts

Steps
1) Add support for setting a `messageId` or similar deduplication header on publish derived from idempotency key or envelope id.
2) Preserve this header on consume; document propagation so consumers can dedupe.
3) Ensure publish and consume paths handle retries without mutating the id.

Acceptance Criteria
- Retries do not create divergent IDs; consumers can perform dedupe reliably with provided header.

Validate
- Publish with duplicate IDs; ensure consumer sees duplicates with same key; verify dedupe behavior in a sample consumer test.

Status Update
- make engineer-done ENGINEER=backend/engineer-02 TASK='BE-02.6a' && make team-status-write

