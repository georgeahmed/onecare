Task: BE-03.2c — Dedup decision function (recent window) + metrics/logs

Context
- Gate duplicate submissions with clear metrics and logs.

Files
- apps/triage/src/application/dedup.ts (new)
- apps/triage/test/dedup.test.ts (new)

Steps
1) Implement `shouldProcess(submission)` using similarity + time window and return decision + reason + matched refs.
2) Emit metrics: dedup.hit/miss counters and histogram for similarity scores.
3) Include correlationId in logs; avoid PHI/PII; redact sensitive fields.

Acceptance Criteria
- Deterministic decisions; metrics and logs present; tests pass.

Validate
- npm run test (dedup.test.ts)

Status Update
- make engineer-done ENGINEER=backend/engineer-03 TASK='BE-03.2c' && make team-status-write

