Task: BE-01.6b — Key/header derivation + TTL config

Context
- Accept explicit idempotency keys and derive sensible defaults; make TTL configurable.

Files
- apps/orchestrator/src/index.ts
- packages/config/src/index.ts

Steps
1) Read `x-idempotency-key`; if absent, derive from a deterministic hash of safe fields + actor (no PHI). Echo key in logs/response headers.
2) TTL via `@onecare/config` (default 10 minutes); expose override via env/practice config.

Acceptance Criteria
- Keys accepted/derived safely; TTL applied; key echoed.

Validate
- Two identical requests produce same derived key; overrides change TTL.

Status Update
- make engineer-done ENGINEER=backend/engineer-01 TASK='BE-01.6b' && make team-status-write

