Task: BE-04.1 — GP Connect client interface + env wiring

Context
- Define GPConnectClient signatures and env for endpoint/keys.

Files
- apps/booking/src/adapters/gpconnect.client.ts (new)

Steps
1) Define interface with searchSlots(params) and createAppointment(appt) signatures.
2) Include options for auth headers (env driven) and timeouts.
3) Export types for Slot and AppointmentRef.

Acceptance Criteria
- Build/typecheck passes; interface imported in booking.state.

Validate
- npm run build && npm run typecheck

Status Update
- make engineer-done ENGINEER=backend/engineer-04 TASK='BE-04.1' && make team-status-write

