Task: BE-07.5 — Observability: routing metrics, ack latency, error rates

Context
- Add structured logs with correlationId, lightweight metrics, and optional spans to make routing and ack behavior observable.

Files
- apps/ics-hub/src/application/ics.state.ts
- packages/observability/src/logger.ts
- packages/observability/src/otel.ts

Steps
1) Ensure correlationId is attached to all logs automatically when set; add counters for `ics.inbound`, `ics.routed`, `ics.ack.published`, `ics.error`.
2) Record a timer metric for ack latency (time from inbound receive to ack publish) and histogram it via logs.
3) Add spans around routing decision and publish operations.

Acceptance Criteria
- Logs consistently include correlationId; metrics counters/timers visible for ICS flows.

Validate
- Simulate referral.request; observe logs include routing and ack metrics.

Status Update
- make engineer-done ENGINEER=backend/engineer-07 TASK='BE-07.5' && make team-status-write

