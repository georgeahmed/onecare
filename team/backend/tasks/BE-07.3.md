Navigation: [Task Index](../../../docs/TASK_INDEX.md) | [All Tasks Flow](../../all-tasks-flow.md) | [Prev](BE-07.2.md) | [Next](BE-07.4.md)

Task: BE-07.3 — ICS ack semantics and idempotency

Context
- Ensure `ics.referral.ack` is published exactly-once per inbound request, even under retries. Include correlationId and stable identifiers.

Files
- apps/ics-hub/src/application/ics.state.ts
- packages/events/src/topics.ts
- packages/ports/src/idempotency.ts

Steps
1) Derive an idempotency key from inbound envelope id (and destination) to ensure a single ack per request.
2) Publish `ics.referral.ack` with `{ requestId, accepted, reason? }` and correlationId; if ack already sent within TTL, skip re-publish.
3) Emit audit intent for ack with minimal details.
4) Add unit test simulating duplicate inbound events → single ack side effect.

Acceptance Criteria
- Duplicate referral.request messages lead to one published ack within TTL.
- Ack payload includes requestId and correlationId; typecheck passes.

Validate
- npm run test

Status Update
- make engineer-done ENGINEER=backend/engineer-07 TASK='BE-07.3' && make team-status-write

