Task: BE-01.5b — Status mapping + HTTP handler wrapper

Context
- Map domain error codes to HTTP status consistently and centralize HTTP error handling with correlation propagation.

Files
- apps/orchestrator/src/application/error.ts
- apps/orchestrator/src/index.ts

Steps
1) Add `mapErrorToStatus(code: ErrorCode): number` → 400/401/403/413/415/429/503/504/500 as per docs.
2) Add `handleHttp(routeName, handler)` wrapper: catches errors, logs once, maps to envelope/status, sets `x-correlation-id`, `cache-control: no-store`.
3) Replace direct try/catch in `/safety-check` with the wrapper.

Acceptance Criteria
- Known codes map to expected statuses; wrapper sets headers and structured error uniformly.

Validate
- Manual curl for invalid input and missing auth; verify 400/403 with correlationId.

Status Update
- make engineer-done ENGINEER=backend/engineer-01 TASK='BE-01.5b' && make team-status-write

