Task: BE-01.4 — Emit audit event on success/deny

Context
- Emit audit.event envelopes at key outcomes (access_denied, success).

Files
- apps/orchestrator/src/index.ts
- packages/ports/src/audit.ts

Steps
1) Define a small helper to create AuditEvent { type, ts, correlationId, payload? }.
2) On deny or success, call AuditLedger.write(event) (stubbed impl ok).
3) Ensure this does not block request completion.

Acceptance Criteria
- Console/log shows audit writes (stub) for both deny and success.
- No runtime errors; typecheck passes.

Validate
- npm run build && npm run typecheck
- Simulate deny (missing auth) and success (SAFE) via /safety-check

Status Update
- make engineer-done ENGINEER=backend/engineer-01 TASK='BE-01.4' && make team-status-write

