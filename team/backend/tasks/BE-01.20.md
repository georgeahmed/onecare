Task: BE-01.20 — Privacy/PII/PHI minimization and redaction

Context
- Enforce strict privacy practices: do not log PHI/PII, minimize event payloads, and respect retention/minimization policies from `config/`. Redact tokens/secrets.

Files
- apps/orchestrator/src/index.ts
- packages/observability/src/logger.ts (reference)
- config/* (retention/minimization policies)
 - docs/CONVENTIONS.md (reference)

Steps
1) Review all logs and remove narratives or free-text fields; log only stable IDs and decision codes. Add a `redact()` helper for structured objects.
2) Ensure envelopes and bus messages do not carry PHI-rich resources; send IDs/refs instead per FHIR guidance.
3) Implement token/secret redaction from headers and envs in logs; never print bearer tokens.
4) Respect retention windows and minimization flags; add docs to README on what data is stored and for how long. Configure log retention and rotation.
5) Add unit tests for redaction helper and log shape.

Acceptance Criteria
- No logs contain PHI/PII or secrets; envelopes share minimal data by design.
- Tests validate redaction; documentation updated for retention/minimization.

Validate
- Grep logs for known PII fields; run unit tests.
 - Verify event payloads exclude PHI beyond stable IDs by inspecting published envelopes in tests.

Status Update
- make engineer-done ENGINEER=backend/engineer-01 TASK='BE-01.20' && make team-status-write
