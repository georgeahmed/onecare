Task: BE-01.5c — Logging redaction + error metrics

Context
- Prevent sensitive data in logs and add basic error metrics for visibility.

Files
- packages/observability/src/logger.ts
- apps/orchestrator/src/application/error.ts

Steps
1) Add redaction utility to strip `authorization`, `cookie`, `set-cookie`, tokens, and IDs from logs.
2) Ensure error logs include `correlationId`, `code`, and safe reason.
3) Emit counters per code (e.g., `error.http.forbidden`) and a duration histogram for request handling.

Acceptance Criteria
- No sensitive fields appear in logs; metrics increment by code.

Validate
- Force 403 and 400 paths; inspect logs/metrics for correctness.

Status Update
- make engineer-done ENGINEER=backend/engineer-01 TASK='BE-01.5c' && make team-status-write

