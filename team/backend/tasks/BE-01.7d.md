Task: BE-01.7d — Wire upsert + audit + unit tests

Context
- Complete the normalization path with FHIR upsert via port and audit outcomes; cover with unit tests.

Files
- packages/ports/src/fhir.ts
- apps/orchestrator/src/index.ts
- apps/orchestrator/test/normalize-flow.test.ts (new)

Steps
1) After normalize+validate, call `FhirRepository.upsertBundle(bundle)`; map failures via error taxonomy and guardrails.
2) Emit audit event on success and denial/failure; ensure non-blocking writes.
3) Add unit tests that stub `FhirRepository` to verify success and failure mappings.

Acceptance Criteria
- Happy path persists bundle and audits; failures map to envelopes; tests pass.

Validate
- Run tests; verify audit and publish side effects only on success.

Status Update
- make engineer-done ENGINEER=backend/engineer-01 TASK='BE-01.7d' && make team-status-write

