Task: BE-04.4 — Contract-first: booking search/request/response and appointment-created schemas; codegen + validators + contract tests

Context
- Define explicit booking contracts before implementation. Generate types for TS/Python and compile validators for ingress and publish paths to prevent drift.

Files
- schemas/booking/booking-search-request.json (new)
- schemas/booking/booking-search-response.json (new)
- schemas/booking/appointment-created.json (new)
- packages/events/src/contracts/*.ts (generated)
- apps/booking/src/index.ts (wire validators)
- docs/EVENTS.md, docs/SCHEMAS.md (reference)

Steps
1) Author the three JSON Schemas with `$id`, `title`, `description`, `additionalProperties: false`, and `format` fields where applicable.
2) Run `npm run codegen` to generate TS contracts; optionally generate Python models.
3) Compile validators at service startup and validate booking.search requests (ingress) and `booking.appointmentCreated` before publish.
4) Add contract tests that load example payloads and validate against schemas.
5) Update `docs/EVENTS.md` with example envelopes and versioning policy for booking.

Acceptance Criteria
- Schemas compile; generated types used by handlers; validators enforced at ingress/publish.
- Contract tests pass with example payloads.

Validate
- npm run codegen && npm run typecheck && npm test

Status Update
- make engineer-done ENGINEER=backend/engineer-04 TASK='BE-04.4' && make team-status-write

