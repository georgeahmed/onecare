Task: BE-02.16 — Multi-tenant isolation & quotas (Bus)

Context
- Ensure per-tenant isolation with partitioning/subjects and enforce throughput quotas to prevent noisy-neighbor impact.

Files
- packages/bus/src/natsBus.ts
- packages/bus/src/types.ts (metadata)

Steps
1) Add tenant-aware subject routing or partition keys; document ordering/per-tenant guarantees.
2) Implement publish-side rate caps per tenant/topic via token-bucket; expose overrides via config.
3) Metrics: `bus.quota.block`, `bus.tenant.throughput`.

Acceptance Criteria
- Per-tenant routing strategy defined; rate caps enforceable and observable.

Validate
- Unit tests for bucket depletion and block behavior.

Status Update
- make engineer-done ENGINEER=backend/engineer-02 TASK='BE-02.16' && make team-status-write

