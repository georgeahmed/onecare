Task: BE-06.7 — Observability for access/capacity (logs, metrics, spans)

Context
- Provide end-to-end observability for access-gate and capacity shaper: structured logs, metrics, and OpenTelemetry spans with correlationId propagation.

Files
- apps/access-gate/src/scheduler.ts
- apps/access-gate/src/application/portal.state.ts
- apps/capacity/src/application/capacity.state.ts
- packages/observability/src/logger.ts, src/otel.ts (reference)

Steps
1) Define standard log fields: `correlationId`, `practiceId`, `event`, `result`. Ensure redaction of sensitive data by default.
2) Emit metrics: `portal.state.changed`, `deferral.enqueue/flush`, `capacity.release.decided`, and latency histograms for key operations.
3) Add OTel spans for `portal.tick`, `portal.ensure`, `deferral.flush`, and `capacity.shape`. Include attributes for `practiceId`, `decision`, `counts`.
4) Ensure `correlationId` is created where none exists and propagated consistently across logs, metrics, and envelopes.
5) Unit tests: assert logger is called with safe structured fields; avoid network I/O.

Acceptance Criteria
- Logs/metrics/spans consistently include correlationId; no PHI/PII.
- Histograms and counters exist for key paths; tests pass.

Validate
- Run locally; inspect logs/spans/metrics output for a few ticks.

Status Update
- make engineer-done ENGINEER=backend/engineer-06 TASK='BE-06.7' && make team-status-write

