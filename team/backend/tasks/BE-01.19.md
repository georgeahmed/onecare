Task: BE-01.19 — Fault injection tests (timeouts, partial failures, slow responses)

Context
- Validate resilience behavior without live networks. Use fakes/mocks to simulate timeouts, transient errors, and slow endpoints; assert retries, backoff, circuit breaker transitions, and fallback paths.

Files
- apps/orchestrator/src/adapters/services/safetyGate.ts (injectable transport)
- apps/orchestrator/src/index.test.ts (new)
- apps/orchestrator/src/adapters/services/safetyGate.test.ts (new)
 - apps/orchestrator/src/adapters/services/callWithGuard.test.ts (new)

Steps
1) Refactor `analyzePortalSubmission` to accept an injectable `transport` or function to simulate responses.
2) Add tests to simulate: timeout → retry → CB open; non-retryable error; 50% failure leading to half-open recovery; slow response that exceeds time budget.
3) Assert metrics increments (`call.retry`, `cb.open`), logged messages, and envelope outcomes.
4) Ensure tests are deterministic and do not perform real network I/O. Use fake timers to control backoff/jitter deterministically.

Acceptance Criteria
- Fault injection suite covers the listed scenarios and passes consistently.
- No network calls in unit tests; behavior matches BE-01.8 guardrail spec.

Validate
- Run tests locally; inspect logs for expected CB state transitions and fallback.

Status Update
- make engineer-done ENGINEER=backend/engineer-01 TASK='BE-01.19' && make team-status-write
