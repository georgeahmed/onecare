Task: BE-01.8b — Circuit breaker (half-open) semantics

Context
- Guard flaky dependencies to fail fast after repeated failures and recover gracefully.

Files
- apps/orchestrator/src/adapters/services/callWithGuard.ts

Steps
1) Add per-dependency circuit breaker: closed → open after failureThreshold; half-open after openMs to probe.
2) Log state transitions with correlationId; surface CB-open as a distinct error code for mapping (`upstream_unavailable`).

Acceptance Criteria
- CB opens after N failures and recovers via half-open; logs show transitions.

Validate
- Tests simulate flapping; assert open/half-open/close progression.

Status Update
- make engineer-done ENGINEER=backend/engineer-01 TASK='BE-01.8b' && make team-status-write

