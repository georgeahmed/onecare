Task: BE-07.8 — Workflow Automation rule engine (config + debouncing)

Context
- Expand the trigger evaluator into a simple, testable rule engine with debounce windows and scheduling constraints.

Files
- apps/ics-hub/src/application/automation.rules.ts
- packages/config/src/index.ts

Steps
1) Support config for rule enablement, thresholds, and schedules (e.g., only create recalls during business hours).
2) Implement debouncing to avoid re-creating the same task within a window (e.g., 7 days) using an idempotency/dedup store.
3) Keep evaluation pure; surface `Intent` objects to adapters for side effects.

Acceptance Criteria
- Rules apply only within configured windows; debounce prevents duplicate creations.

Validate
- Unit tests verify debouncing and schedule adherence.

Status Update
- make engineer-done ENGINEER=backend/engineer-07 TASK='BE-07.8' && make team-status-write

