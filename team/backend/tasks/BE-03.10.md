Task: BE-03.10 — Observability (correlation, metrics, spans)

Context
- Provide visibility into triage pipeline performance and reliability. Propagate `correlationId` and expose structured logs, metrics, and optional spans.

Files
- packages/observability/src/logger.ts
- packages/observability/src/otel.ts
- apps/triage/src/index.ts

Steps
1) Ensure `correlationId` flows from consumed envelope to all logs and subsequent publishes. Add helpers to annotate contexts.
2) Metrics: counters for scoring decisions by priority; dedup hit/miss; assignment distribution; task create/update success/error; processing latency histograms.
3) Wrap pipeline segments in spans (`score`, `dedup`, `task.create`, `assign`, `notify`). Include minimal attributes; avoid PHI.
4) Add unit tests that assert logging with correlationId and metric increments using fakes.

Acceptance Criteria
- Logs/metrics/spans consistently include `correlationId` and safe fields.
- Tests validate observability hooks without external systems.

Validate
- Local run with stub exporter; inspect console output.

Status Update
- make engineer-done ENGINEER=backend/engineer-03 TASK='BE-03.10' && make team-status-write

