Task: BE-05.7 — Patient notification adapter

Context
- Notify patients with minimal, PHI-safe payloads; enforce idempotency and backoff. Check consent before notify.

Files
- packages/ports/src/queue.ts (notify abstraction)
- apps/pharmacy-router/src/application/pharmacy.state.ts

Steps
1) Define `notify(channel, message)` abstraction; stub or log-only in dev.
2) Idempotency: dedupe notifications by (referralId, channel, templateCode).
3) Backoff on transient failures; correlationId propagation; consent check via port/stub.

Acceptance Criteria
- Notifications idempotent; consent enforced; minimal payloads and safe logs.

Validate
- Unit tests for notify dedupe and consent denial path.

Status Update
- make engineer-done ENGINEER=backend/engineer-05 TASK='BE-05.7' && make team-status-write

