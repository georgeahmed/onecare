Task: BE-02.14 — Adapter parity tests vs MemoryBus

Context
- Ensure the real bus adapter behaves the same as MemoryBus for core semantics to simplify testing and migration.

Files
- packages/bus/src/memoryBus.ts (reference)
- packages/bus/src/natsBus.ts
- packages/bus/test/*.test.ts (new)

Steps
1) Define a contract test suite that runs against both adapters: publish/subscribe ordering by key, dedupe/idempotency wrapper behavior, DLQ/retry flows.
2) Keep tests deterministic with fakes and without network for MemoryBus; integration tests may spin a local NATS instance.

Acceptance Criteria
- Contract suite passes for both adapters; documented differences (if any) are minimal and acceptable.

Validate
- npm run test (unit); optional docker-compose integration for NATS tests.

Status Update
- make engineer-done ENGINEER=backend/engineer-02 TASK='BE-02.14' && make team-status-write

