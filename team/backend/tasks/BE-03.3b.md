Task: BE-03.3b — Emit tasks.created; contract tests and validators

Context
- After Task creation, emit `tasks.created` with minimal payload and ensure contract compliance.

Files
- schemas/tasks/task-created.json
- packages/events/src/contracts/task-created.ts (generated)
- apps/triage/src/application/publish.ts (new)

Steps
1) Build `tasks.created` payload with stable IDs and minimal fields; validate against compiled schema before publish.
2) Use `createEnvelope(Topics.tasks.created, payload, correlationId)` and publish via injected bus; retry with guardrails.
3) Add contract tests for sample payloads and envelope shape.

Acceptance Criteria
- Envelope and payload validate against schemas; publish retries bounded; correlationId propagated.

Validate
- npm run codegen && npm run test

Status Update
- make engineer-done ENGINEER=backend/engineer-03 TASK='BE-03.3b' && make team-status-write

