Task: BE-07.1 — ICS ingress validation + routing policy skeleton

Context
- Validate `ics.referral.request` envelopes and payloads, decide routing with a config-driven policy, and prepare ack + audit hooks (ack semantics completed in BE-07.3).

Files
- apps/ics-hub/src/application/ics.state.ts
- packages/events/src/topics.ts (reference)
- packages/events/src/contracts (ICS contracts via codegen)

Steps
1) Add a pure `buildRouteDecision(request)` function (no I/O) that consumes a typed referral request and returns `{ destinationOrgId, policy, rationale }` using a config mapping (env/JSON), with a safe default.
2) Validate inbound envelope shape (id, topic, payload, correlationId) and payload according to generated TS types; return early Decision objects on invalid input (no throws in business logic).
3) In the ICS state machine, wire `Inbound → Validated → Routed` transitions to use the validation + decision and stash routing metadata in context; do not publish yet.
4) Emit audit intents (not writes) on inbound receipt and routing decision; actual write handled via adapter in later tasks.
5) Add unit tests for decision function and state transitions (happy path + invalid payload path).

Acceptance Criteria
- Given a valid referral request, the state machine reaches `Routed` with routing metadata in context.
- Invalid input yields a terminal Decision with an error code; no publish side effects in this task.
- Typecheck passes; unit tests cover core paths.

Validate
- npm run build && npm run typecheck && npm run test

Status Update
- make engineer-done ENGINEER=backend/engineer-07 TASK='BE-07.1' && make team-status-write
