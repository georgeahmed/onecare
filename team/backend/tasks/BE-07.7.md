Task: BE-07.7 — DLQ handling and reprocessing flow

Context
- Provide a safe, operator-friendly flow to reprocess DLQ messages with idempotency and observability.

Files
- apps/ics-hub/src/application/ics.state.ts
- apps/ics-hub/src/dev/replay.ts (new)
- packages/events/src/topics.ts

Steps
1) Define the DLQ message shape (topic, payloadRef, error, correlationId, ts).
2) Add a small dev/ops utility to replay DLQ items through the normal ingress with an override correlationId.
3) Ensure reprocessing respects idempotency keys to avoid duplicates; log replays with a distinct flag.

Acceptance Criteria
- DLQ items can be safely replayed; duplicates are not re-emitted.

Validate
- Simulate DLQ entry, run replay utility, observe correct routing and ack.

Status Update
- make engineer-done ENGINEER=backend/engineer-07 TASK='BE-07.7' && make team-status-write

