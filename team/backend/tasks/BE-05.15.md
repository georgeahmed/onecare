Task: BE-05.15 — Rate limits & backpressure (pharmacy)

Context
- Protect referral and outcome endpoints/consumers from overload with rate limiting and concurrency caps.

Files
- apps/pharmacy-router/src/index.ts
- packages/config/src/index.ts

Steps
1) Implement per-tenant token-bucket rate limits with configurable rates and bursts; return 429 on breach with `retry-after`.
2) Add a concurrency semaphore for referral processing; return 503 when saturated with a safe envelope.
3) Emit metrics `ratelimit.hit/miss`, `backpressure.queue/reject`.

Acceptance Criteria
- Limits enforced; metrics/logs present.

Validate
- Burst tests locally; verify controlled rejects and stable latencies.

Status Update
- make engineer-done ENGINEER=backend/engineer-05 TASK='BE-05.15' && make team-status-write

