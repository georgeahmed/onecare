Task: BE-07.12 — Audit ledger resiliency (ICS + Automation)

Context
- Make audit writes non-blocking and resilient with timeouts and a bounded spool.

Files
- packages/ports/src/audit.ts
- apps/ics-hub/src/application/ics.state.ts

Steps
1) Wrap audit writes with a small async queue and timeout + limited retries.
2) On repeated failure, push to a bounded in-memory spool and emit a metric; flush when downstream recovers.
3) Ensure enqueue never blocks routing or ack.

Acceptance Criteria
- Audit sink failures do not delay routing/ack; spool drains on recovery.

Validate
- Simulate failures; verify spooling and later flush via logs/metrics.

Status Update
- make engineer-done ENGINEER=backend/engineer-07 TASK='BE-07.12' && make team-status-write

