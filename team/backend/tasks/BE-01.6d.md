Task: BE-01.6d — Concurrency tests (sequential + parallel)

Context
- Prove race-safety under parallel identical requests and ensure only one proceeds.

Files
- apps/orchestrator/test/idempotency.test.ts (new)

Steps
1) Sequential: Post twice with same key; expect 200 then 409 with no side effects on second call.
2) Parallel: Fire N=5 concurrent requests with same key; exactly one proceeds, N-1 return 409.
3) Assert metrics/logs for hits/misses and that side effects only occur once.

Acceptance Criteria
- Deterministic test proves exactly one winner; no duplicate side effects.

Validate
- Run tests; inspect logs for correlationId and idempotency key.

Status Update
- make engineer-done ENGINEER=backend/engineer-01 TASK='BE-01.6d' && make team-status-write

