Task: BE-03.1a — Feature extraction + normalization (pure; unit-tested)

Context
- Extract triage features (acuity, risk, complexity, time, capacity) from inputs and normalize them to stable ranges prior to scoring.

Files
- apps/triage/src/application/features.ts (new)
- apps/triage/test/features.test.ts (new)
- config/nhs_gp_defaults.yaml (triage normalization params)

Steps
1) Define `extractFeatures(input, ctx)` returning a typed struct { acuity, risk, complexity, timePressure, capacityPressure }.
2) Implement normalization functions (min-max or z-score) using config floors/ceilings and defaults; ensure pure and deterministic.
3) Add fixtures covering typical and edge inputs (missing values, extremes).

Acceptance Criteria
- Unit tests pass; feature outputs are deterministic and bounded as per config.
- No I/O or side effects; pure functional implementation.

Validate
- npm run test (features.test.ts)

Status Update
- make engineer-done ENGINEER=backend/engineer-03 TASK='BE-03.1a' && make team-status-write

