Task: BE-04.7 — Outbound guardrails (GP Connect/FHIR)

Context
- Guard external calls with timeouts, retries, jitter, and circuit breaker; enforce SSRF allowlists and propagate correlationId.

Files
- apps/booking/src/adapters/gpconnect.client.ts
- packages/ports/src/fhir.ts
- apps/booking/src/application/callWithGuard.ts (shared util or reuse existing)

Steps
1) Wrap GP Connect/FHIR calls with `callWithGuard` (2s timeout, 2 retries, baseDelay 100ms, CB).
2) Add SSRF allowlist checks: block private/loopback IPs; allow hostnames via config; TLS required when applicable.
3) Propagate `x-correlation-id` and `traceparent` headers; add metrics (`call.retry`, `call.timeout`, `cb.open`).
4) On CB open: return friendly error and avoid hammering.

Acceptance Criteria
- Guardrails applied; SSRF checks pass; correlation propagates.

Validate
- Fault injection tests: timeouts, retries, CB transitions; SSRF blocks loopback.

Status Update
- make engineer-done ENGINEER=backend/engineer-04 TASK='BE-04.7' && make team-status-write

