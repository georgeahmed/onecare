Task: BE-03.2b — Similarity (shingles/MinHash or cosine) with configurable window/threshold + eviction policy

Context
- Detect duplicates within a configurable time window using reliable, fast similarity.

Files
- apps/triage/src/application/similarity.ts (new)
- apps/triage/test/similarity.test.ts (new)

Steps
1) Implement shingling + MinHash (or TF-IDF + cosine) to compute similarity scores; make n-gram size, threshold, and stopword list configurable.
2) Track recent submissions in a time-windowed store with eviction; ensure memory bounds.
3) Provide a `isDuplicate(normalizedText, now)` decision and return matched refs and scores for audit.

Acceptance Criteria
- Deterministic similarity decisions; bounded memory; tests for false positives/negatives and thresholds.

Validate
- npm run test (similarity.test.ts)

Status Update
- make engineer-done ENGINEER=backend/engineer-03 TASK='BE-03.2b' && make team-status-write

