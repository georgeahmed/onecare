Task: BE-01.7a — normalizeToFhir + fixtures (pure)

Context
- Build a pure, deterministic mapper from PortalSubmission to a minimal FHIR Bundle without I/O.

Files
- apps/orchestrator/src/application/normalize.ts (new)
- apps/orchestrator/src/application/normalize.test.ts (new)

Steps
1) Implement `normalizeToFhir(submission)` → FHIR transaction Bundle with minimal entries (Patient ref, Communication, DocumentReference refs if attachments).
2) Avoid side effects and external lookups; ensure IDs are stable across runs for same input.
3) Add fixtures for typical and edge cases; unit tests validate structure.

Acceptance Criteria
- Mapping pure and repeatable; bundle entries as expected; tests pass.

Validate
- npm run test on normalize tests; snapshot small bundle shapes.

Status Update
- make engineer-done ENGINEER=backend/engineer-01 TASK='BE-01.7a' && make team-status-write

