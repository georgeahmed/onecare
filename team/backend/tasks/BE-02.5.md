Task: BE-02.5 — Connection resilience and backoff

Context
- Keep connections stable under broker restarts and network blips. Avoid thundering herds on reconnect and surface status clearly.

Files
- packages/bus/src/natsBus.ts
- apps/orchestrator/src/index.ts (status wiring)

Steps
1) Enable reconnect with exponential backoff + jitter (min/max). Surface reconnect/close events via callbacks or an EventEmitter.
2) Emit metrics: `bus.reconnects`, `bus.disconnects`, `bus.uptime`, and histogram for reconnect delay.
3) Log connection state transitions with correlation-free fields (no PHI).
4) Make backoff parameters configurable via env/@onecare/config; document sane defaults.

Acceptance Criteria
- Reconnect occurs with jittered backoff; metrics/logs reflect transitions.

Validate
- Restart broker locally; observe reconnect behavior and metrics.

Status Update
- make engineer-done ENGINEER=backend/engineer-02 TASK='BE-02.5' && make team-status-write

