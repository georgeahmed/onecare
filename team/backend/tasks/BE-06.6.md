Task: BE-06.6 — Scheduler resilience & idempotency (singleton, jitter, skew tolerance)

Context
- Make scheduler loops resilient across replicas. Prevent duplicate work, add jitter to avoid thundering herds, and tolerate small clock skews. Ensure handlers are idempotent.

Files
- apps/access-gate/src/scheduler.ts
- apps/capacity/src/application/capacity.state.ts (tick wiring, if applicable)

Steps
1) Add ±5–10s jitter to periodic ticks. Use a monotonic clock for measuring intervals to avoid wall-clock jumps.
2) Introduce a short-lived in-process singleflight lock per `practiceId` to collapse concurrent ticks; document that infra-level leader election may replace this later.
3) Make tick handlers re-entrant and idempotent: use idempotency keys and current-state checks before side effects.
4) Add metrics: `scheduler.tick`, `scheduler.skip.singleflight`, and `scheduler.idempotent.noop`.
5) Unit tests: simulate concurrent ticks and clock drift; assert single execution and no duplicates.

Acceptance Criteria
- Concurrent ticks collapse to single execution; idempotent handlers produce no duplicate side effects.
- Metrics reflect skipped/merged ticks; tests deterministic and fast.

Validate
- npm test

Status Update
- make engineer-done ENGINEER=backend/engineer-06 TASK='BE-06.6' && make team-status-write

