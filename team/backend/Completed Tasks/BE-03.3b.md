Navigation: [Task Index](../../../docs/TASK_INDEX.md) | [All Tasks Flow](../../all-tasks-flow.md) | [Prev](BE-03.3a.md) | [Next](BE-03.4.md)

Task: BE-03.3b — Emit tasks.created; contract tests and validators

Context
- After Task creation, emit `tasks.created` with minimal payload and ensure contract compliance.

Files
- schemas/tasks/task-created.json
- packages/events/src/contracts/task-created.ts (generated)
- apps/triage/src/application/publish.ts (new)

Steps
1) Compile the `task-created` schema into a reusable validator and assert payloads prior to publish (reject with safe error when invalid).
2) Centralise publishing via a helper that wraps `createEnvelope(...)` and applies bounded retries/backoff + correlation headers using bus publish options.
3) Add contract tests (happy + failure) that exercise the validator and confirm envelope shape/metadata.

Acceptance Criteria
- Envelope and payload validate against schemas; publish retries bounded; correlationId propagated.

Validate
- npm run codegen && npm run test

Status Update
- make engineer-done ENGINEER=backend/engineer-03 TASK='BE-03.3b' && make team-status-write
