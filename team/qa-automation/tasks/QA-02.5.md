Task: QA-02.5 — CI integration with SLO thresholds + artifacts

Context
- Wire performance checks into CI with SLO thresholds.

Steps
1) Add a CI job to run the perf scripts (or smoke) and check thresholds.
2) Upload metrics as artifacts for review.

Acceptance Criteria
- CI shows perf job; passes when under thresholds.

Validate
- Push PR and observe CI (or dry-run locally).

Status Update
- make engineer-done ENGINEER=qa-automation/engineer-02 TASK='QA-02.5' && make team-status-write

