Task: QA-01.3 — E2E test: portal submission → orchestrator → triage Task

Context
- Validate the main path from intake to Task creation with correlation propagation and negative cases.

Steps
1) Start stack (or mock) and submit a sample submission to /safety-check with `x-correlation-id` header.
2) Assert triage.input published and tasks.created eventually emitted (dev bus/mocks ok) and that `correlationId` is preserved.
3) Negative case: Safety gate DIVERTED → assert no triage.input is published.

Acceptance Criteria
- E2E passes locally.

Validate
- make stack-up && make stack-smoke (or run dedicated test)

Status Update
- make engineer-done ENGINEER=qa-automation/engineer-01 TASK='QA-01.3' && make team-status-write
