Task: QA-01.1 — Schema validation harness for TS events (Ajv)

Context
- Validate event payloads and HTTP error envelopes against JSON Schemas with compiled validators and clear error messages.

Steps
1) Add a small TS utility using Ajv to compile schemas from `schemas/`; cache compiled validators per `$id`.
2) Provide positive/negative tests validating triage.input, appointment.created, error-envelope, and dlq-event samples.
3) Ensure errors report path and keyword; snapshots assert error shapes (not messages) for stability.

Acceptance Criteria
- Valid samples pass; invalid samples produce deterministic error lists.

Validate
- npm run test

Status Update
- make engineer-done ENGINEER=qa-automation/engineer-01 TASK='QA-01.1' && make team-status-write
