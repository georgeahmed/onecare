Task: QA-01.18 — Contract drift monitor (schema/codegen diff gates; snapshots)

Context
- Prevent silent contract drift by gating changes: schema vs generated types vs validators must remain in sync.

Files
- .github/workflows/ci.yml (ensure codegen:check)
- qa/contracts/snapshots/* (new)

Steps
1) Add CI step to run `npm run codegen:check`; fail if generated artifacts are outdated.
2) Maintain snapshots of representative payload shapes; update snapshots only with review and schema bump.
3) Document the process for schema evolution and updating tests.

Acceptance Criteria
- CI catches drift; snapshots serve as guardrails; docs in place.

Validate
- PR with schema change but no codegen must fail.

Status Update
- make engineer-done ENGINEER=qa-automation/engineer-01 TASK='QA-01.18' && make team-status-write

