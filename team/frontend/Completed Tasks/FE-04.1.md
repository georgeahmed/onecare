Navigation: [Task Index](../../../docs/TASK_INDEX.md) | [All Tasks Flow](../../all-tasks-flow.md)

Task: FE-04.1 — Scaffold clinician dashboard app (apps/clinician)

Context
- Create a new clinician dashboard SPA separate from the patient Portal.

Files
- apps/clinician/package.json, tsconfig.json, vite.config.ts, src/main.tsx, src/App.tsx
- apps/clinician/src/styles/{tokens.css,global.css}

Steps
1) Scaffold a Vite + React + TS app under `apps/clinician` with project references.
2) Add basic app shell and route placeholder (`/queue`, `/case/:id`, `/settings`).
3) Wire lint/test/typecheck tasks; add workspace scripts.

Acceptance Criteria
- App builds locally and mounts at `/` with placeholder routes.
- Typecheck passes; workspace wired in root `tsconfig.json` and `package.json`.

Validate
- `npm -w @onecare/app-clinician run build` and open preview; confirm route placeholders.

Status Update
- make engineer-done ENGINEER=frontend/engineer-04 TASK='FE-04.1' && make team-status-write

