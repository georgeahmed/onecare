Navigation: [Task Index](../../../docs/TASK_INDEX.md) | [All Tasks Flow](../../all-tasks-flow.md)

Task: FE-04.4 — Data adapters: dev MockQueue + API gateway interface

Context
- Backend query endpoints for triage may arrive later. Provide a mock adapter for dev and a pluggable API gateway.

Files
- apps/clinician/src/adapters/queue.types.ts
- apps/clinician/src/adapters/queue.mock.ts
- apps/clinician/src/adapters/gateway.ts

Steps
1) Define `QueueGateway` interface (list, assign, unassign, get by id).
2) Implement `MockQueueGateway` with seeded data; simulate delay and conflicts.
3) Provide a gateway setter so production can swap in an API-backed implementation with correlation headers.

Acceptance Criteria
- App reads from mock gateway in dev; gateway switch via env.
- Errors map to friendly messages consistent with docs/ERRORS.md.

Validate
- Unit tests for gateway contract; mock timing and conflict paths covered.

Status Update
- make engineer-done ENGINEER=frontend/engineer-04 TASK='FE-04.4' && make team-status-write
