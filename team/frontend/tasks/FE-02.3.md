Task: FE-02.3 — Slot selection → idempotent booking confirmation

Context
- Provide a clear confirm step that prevents accidental double booking by using idempotency keys and disabling duplicate actions.

Files
- apps/portal/src/components/booking/ConfirmBooking.tsx (new)

Steps
1) On "Select", navigate to a confirmation view summarizing details; generate idempotency key (slotId+patientId+timestamp hash) and store in state.
2) On "Confirm", POST with `x-idempotency-key` and `x-correlation-id`; disable button and show progress; handle success/rollback.
3) Restore UI safely on failure; never lose selected slot context; provide back navigation.

Acceptance Criteria
- Exactly-once user action UX (no double-submits); confirmation clear and accessible.

Validate
- Manual double-click test; unit tests for idempotency key generation and UI disabling.

Status Update
- make engineer-done ENGINEER=frontend/engineer-02 TASK='FE-02.3' && make team-status-write

