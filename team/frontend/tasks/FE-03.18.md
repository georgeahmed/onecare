Task: FE-03.18 — Fallback logic & offline locale cache

Context
- Ensure i18n remains functional offline with cached resources and graceful fallbacks.

Files
- apps/portal/src/i18n/*
- apps/portal/src/sw.ts (if PWA)

Steps
1) Cache locale bundles via service worker or localStorage; verify stale‑while‑revalidate update pattern.
2) Provide a minimal built‑in English fallback for critical UI if locale fails to load.
3) Log safe telemetry if fallbacks used; prompt refresh when updated.

Acceptance Criteria
- Offline renders with cached locale; fallback only when necessary; UX remains coherent.

Validate
- Offline simulation; switch locales; observe caching behavior.

Status Update
- make engineer-done ENGINEER=frontend/engineer-03 TASK='FE-03.18' && make team-status-write

