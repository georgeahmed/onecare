Navigation: [Task Index](../../../docs/TASK_INDEX.md) | [All Tasks Flow](../../all-tasks-flow.md)

Task: FE-04.2 — Auth guard, RBAC, and clinic switcher

Context
- Multi‑clinic app must scope users to their clinic(s) and roles (clinician, coordinator, admin).

Files
- apps/clinician/src/lib/auth.ts, src/hooks/useAuth.ts, src/components/ClinicSwitcher.tsx
- apps/clinician/src/routes/ProtectedRoute.tsx

Steps
1) Implement a simple auth context and guard (stub token in dev); roles: clinician, coordinator, admin.
2) Add clinic switcher (if user has >1 clinic) with active clinic stored client‑side.
3) Ensure routes are protected; redirect unauthenticated to `/login` placeholder.

Acceptance Criteria
- Visiting protected routes without auth redirects to `/login`.
- Active clinic badge/switcher visible; subsequent views filter by clinic.

Validate
- Unit tests for guard and clinic switcher behavior.

Status Update
- make engineer-done ENGINEER=frontend/engineer-04 TASK='FE-04.2' && make team-status-write

