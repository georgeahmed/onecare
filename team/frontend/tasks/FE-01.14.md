Task: FE-01.14 — Offline-ready PWA (service worker, background sync, offline queue)

Context
- Handle spotty networks gracefully: cache shell, queue submissions offline, and retry with backoff after reconnect.

Files
- apps/portal/src/sw.ts (service worker)
- apps/portal/src/lib/offlineQueue.ts (new)
- docs/USAGE.md (PWA)

Steps
1) Add a basic service worker for app shell and critical assets; cache versioning strategy.
2) Implement an offline queue for form submissions: persist payload + correlationId; background sync/retry with jitter when online.
3) Show an offline indicator and human‑friendly messaging; never lose user input.
4) Respect privacy: do not store narratives longer than necessary; encrypt or minimize where possible.

Acceptance Criteria
- App works offline for core shell; submissions queued and retried; user informed without nagging.

Validate
- Simulate offline in dev tools; submit; reconnect and verify send.

Status Update
- make engineer-done ENGINEER=frontend/engineer-01 TASK='FE-01.14' && make team-status-write

