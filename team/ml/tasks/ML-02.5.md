Task: ML-02.5 — Save/load model artifact with versioning

Context
- Persist artifacts with version metadata.

Steps
1) Save artifact as models/acuity-v{semver}.bin and latest symlink.
2) Add simple loader that reads version metadata.
3) Document version bump process.

Acceptance Criteria
- Loader returns artifact and version.

Validate
- pytest -q services-py/tests

Status Update
- make engineer-done ENGINEER=ml/engineer-02 TASK='ML-02.5' && make team-status-write

