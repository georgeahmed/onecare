Task: ML-02.1 — Define feature schema + encoding utilities

Context
- Provide consistent features for acuity model (symptom embeddings, age, comorbid).

Steps
1) Define features.json (keys and types) for triage/acuity.
2) Implement encode_features.py: encode(raw) → feature vector (list/dict).
3) Unit: given raw sample, encoded shapes match.

Acceptance Criteria
- Unit passes; schema documented.

Validate
- pytest -q services-py/tests

Status Update
- make engineer-done ENGINEER=ml/engineer-02 TASK='ML-02.1' && make team-status-write

