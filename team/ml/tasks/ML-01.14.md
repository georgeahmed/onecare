Task: ML-01.14 — Concurrency & backpressure (worker limits, time budgets, 429/503)

Context
- Protect the service under load with bounded concurrency, time budgets, and graceful overload responses.

Files
- services-py/safety_gate_service/main.py

Steps
1) Configure worker/concurrency limits (uvicorn/gunicorn + in-app semaphore) and an overall request time budget.
2) On queue saturation, respond with 429 (Too Many Requests) with a safe envelope; when budgets exceeded, return 503.
3) Emit metrics for queue length, dropped/limited requests; log correlationId.

Acceptance Criteria
- Under load, concurrency stays bounded; overload responses are correct; metrics visible.

Validate
- Synthetic load (e.g., locust or simple asyncio flood) and unit tests for queue behavior.

Status Update
- make engineer-done ENGINEER=ml/engineer-01 TASK='ML-01.14' && make team-status-write

