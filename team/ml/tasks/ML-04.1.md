Task: ML-04.1 — LLM client selection + env wiring (model, keys)

Context
- Choose LLM and wire client with env.

Steps
1) Implement llm_client.py with class SummaryLLM(model_name, api_key).
2) Read env LLM_MODEL and LLM_API_KEY.
3) Method summarize(transcript, max_tokens) → summary (stub ok).

Acceptance Criteria
- Unit passes; env read documented.

Validate
- pytest -q services-py/tests

Status Update
- make engineer-done ENGINEER=ml/engineer-04 TASK='ML-04.1' && make team-status-write

