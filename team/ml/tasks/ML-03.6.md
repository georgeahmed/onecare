Task: ML-03.6 — Audio retention controls

Context
- Respect ambient_scribe.store_audio and retention_days for audio artifacts.

Files
- services-py/scribe_service/main.py

Steps
1) If store_audio==binary, persist audio reference; else discard.
2) Implement retention policy (delete after N days) as a stub (docs and function scaffolding).
3) Unit: when disabled, no audio refs stored; when enabled, refs present.

Acceptance Criteria
- Tests pass; behavior matches config.

Validate
- pytest -q services-py/tests

Status Update
- make engineer-done ENGINEER=ml/engineer-03 TASK='ML-03.6' && make team-status-write

