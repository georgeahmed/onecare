Task: ML-04.4 — Enforce MAX_SUMMARY_TOKENS + truncation strategy

Context
- Keep output within token budget.

Steps
1) Add truncate_to_tokens(text, max_tokens) helper (rough counter ok).
2) Apply before sending to LLM and post-process.
3) Unit: length under threshold.

Acceptance Criteria
- Tests pass.

Validate
- pytest -q services-py/tests

Status Update
- make engineer-done ENGINEER=ml/engineer-04 TASK='ML-04.4' && make team-status-write

