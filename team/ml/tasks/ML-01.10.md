Navigation: [Task Index](../../../docs/TASK_INDEX.md) | [All Tasks Flow](../../all-tasks-flow.md) | [Prev](ML-01.1.md) | [Next](ML-01.11.md)

Task: ML-01.10 — Observability: correlationId propagation, Prometheus metrics, OTel spans

Context
- Propagate `x-correlation-id` in logs/metrics; expose Prometheus metrics; add optional OTel spans around inference.

Files
- services-py/safety_gate_service/main.py

Steps
1) Read `x-correlation-id` from request; include it in logs and response header.
2) Expose `/metrics` via Prometheus FastAPI/Starlette exporter; include request counters and latency histograms.
3) Wrap NER/classify/decision calls in spans when OTel is available; otherwise no-op.

Acceptance Criteria
- CorrelationId visible in logs and echoed; metrics endpoint returns counters/histograms; spans created in dev.

Validate
- curl /metrics; run pytest to confirm headers/log fields.

Status Update
- make engineer-done ENGINEER=ml/engineer-01 TASK='ML-01.10' && make team-status-write

