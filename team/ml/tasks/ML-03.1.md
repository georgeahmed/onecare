Task: ML-03.1 — Whisper model download + runner stub

Context
- Prepare ASR runner for transcripts.

Steps
1) Add asr_runner.py with load_model() (stub path) and transcribe(audioRef).
2) Handle long audio chunking at a basic level (to be improved later).
3) Unit: mock runner returns fixed transcript.

Acceptance Criteria
- Tests pass; runner interface stable.

Validate
- pytest -q services-py/tests

Status Update
- make engineer-done ENGINEER=ml/engineer-03 TASK='ML-03.1' && make team-status-write

