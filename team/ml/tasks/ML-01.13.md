Navigation: [Task Index](../../../docs/TASK_INDEX.md) | [All Tasks Flow](../../all-tasks-flow.md) | [Prev](ML-01.12.md) | [Next](ML-01.14.md)

Task: ML-01.13 — Robust error envelopes (no stack traces); input limits and validation

Context
- Ensure safe error handling and input guardrails; align with `docs/ERRORS.md` envelope pattern.

Files
- services-py/safety_gate_service/main.py

Steps
1) Define an error envelope `{ error: { code, message, details?, correlationId } }`; map exceptions to 4xx/5xx codes; never return stack traces.
2) Enforce `Content-Type: application/json`, size limits (e.g., 64KB default), and body shape validation; return 413/415/400 as appropriate.
3) Unit tests: invalid content-type, oversized body, invalid schema, and unexpected error paths return correct envelopes.

Acceptance Criteria
- Error envelopes consistent with docs; size/type limits enforced; tests pass.

Validate
- pytest; manual curl.

Status Update
- make engineer-done ENGINEER=ml/engineer-01 TASK='ML-01.13' && make team-status-write

