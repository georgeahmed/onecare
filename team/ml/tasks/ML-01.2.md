Navigation: [Task Index](../../../docs/TASK_INDEX.md) | [All Tasks Flow](../../all-tasks-flow.md) | [Prev](ML-01.19.md) | [Next](ML-01.20.md)

Task: ML-01.2 — NER model setup (BioClinicalBERT) + inference wrapper

Context
- Extract symptoms/entities from text for Safety Gate with thread-safe, device-aware inference.

Files
- services-py/safety_gate_service (module)

Steps
1) Create `ner.py` with class `SafetyNER` wrapping a HF pipeline; support `device=cpu|cuda` and lazy load (single shared instance).
2) Provide `analyze(text)` → `{ symptoms: [], severity: [], temporal: [], context_entities: [] }`; ensure thread-safety (no global mutable state leaks).
3) Add unit tests for CPU mode; if offline, provide stub mode gated by env to keep tests deterministic.

Acceptance Criteria
- Unit passes; types consistent.

Validate
- pytest -q services-py/tests

Status Update
- make engineer-done ENGINEER=ml/engineer-01 TASK='ML-01.2' && make team-status-write
