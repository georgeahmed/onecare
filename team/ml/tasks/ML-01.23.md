Task: ML-01.23 — Determinism/reproducibility (seeds, pinned deps, model card + hashes)

Context
- Make experiments and deployments reproducible and auditable.

Files
- services-py/safety_gate_service/README.md (model card)
- requirements.txt/poetry.lock (pins)
- docs/adr/2025-..-safety-gate-reproducibility.md (new)

Steps
1) Set seeds for PyTorch/NumPy/random; disable non-deterministic ops or document exceptions; record seed in logs.
2) Pin dependencies; generate SBOM; include model file hashes and sizes; verify on startup.
3) Author a short model card: data sources (non-PHI), intended use, limitations, metrics, thresholds, known biases.
4) Document experiment tracking basics (tags, artifacts) and folder structure.

Acceptance Criteria
- Seeds set; deps pinned; model card present; startup verifies hashes; ADR recorded.

Validate
- Manual inspection; CI check for pinned deps and SBOM generation.

Status Update
- make engineer-done ENGINEER=ml/engineer-01 TASK='ML-01.23' && make team-status-write

