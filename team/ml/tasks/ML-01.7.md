Task: ML-01.7 — Latency budget checks (p50/p95) + metrics/observability

Context
- Enforce latency SLOs and expose metrics/traces: Prometheus endpoint for FastAPI, and optional OTel spans.

Steps
1) Add timing around `/analyze`; compute rolling p50/p95 over a sliding window and expose counters/histograms via a Prometheus `/metrics` endpoint.
2) Warn when p95 exceeds configured SLO; include `correlationId` in logs; start spans if OTel available.
3) Unit: fake timings and confirm metrics/log output format (shape-only assertions).

Acceptance Criteria
- Metrics endpoint returns histograms/counters; logs include SLO warnings and correlationId; unit passes.

Validate
- pytest -q services-py/tests

Status Update
- make engineer-done ENGINEER=ml/engineer-01 TASK='ML-01.7' && make team-status-write
