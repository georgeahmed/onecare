Task: ML-01.9 — Contracts-first models and validators (schemas, codegen, compiled validators)

Context
- Align API contracts with `schemas/`; generate Pydantic models and compile validators for performance.

Files
- services-py/common/contracts (generated)
- schemas/ingest/portal-submission.json, schemas/safety/safety-decision.json

Steps
1) Ensure schemas exist/updated; run `RUN_PY=1 npm run codegen` to generate Pydantic models.
2) Add lightweight compiled validators (e.g., `pydantic.TypeAdapter`/validate_json) for request body.
3) Unit tests: valid/invalid cases; no network.

Acceptance Criteria
- Codegen artifacts present; validation path uses generated models; tests pass.

Validate
- pytest -q services-py/tests

Status Update
- make engineer-done ENGINEER=ml/engineer-01 TASK='ML-01.9' && make team-status-write

