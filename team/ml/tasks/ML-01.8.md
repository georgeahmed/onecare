Task: ML-01.8 — PII redaction in logs + sampling policy

Context
- Ensure logs do not include PHI/PII; document log sampling policy.

Files
- services-py/safety_gate_service/main.py

Steps
1) Implement a logger that redacts names/phones/etc. (basic regex acceptable for stub).
2) Add sampling policy: only log N% of requests in dev; none in prod (config-driven).
3) Unit: verify redaction replaces sensitive tokens.

Acceptance Criteria
- Tests pass; logs are redacted.

Validate
- pytest -q services-py/tests

Status Update
- make engineer-done ENGINEER=ml/engineer-01 TASK='ML-01.8' && make team-status-write

