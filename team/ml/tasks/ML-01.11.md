Task: ML-01.11 — Health/readiness probes + warmup (model load, cold-start budget)

Context
- Provide `/health` and `/ready` endpoints; load/initialize models at startup with warmup to reduce cold-start latency.

Files
- services-py/safety_gate_service/main.py

Steps
1) Add `/health` (always 200) and `/ready` (200 after model(s) loaded + warmup); include version and model hash.
2) Preload/initialize models on startup; run a small warmup pass; log load and warm times with correlation-free fields.
3) Document expected cold-start budget; add unit test stubs that simulate readiness flips.

Acceptance Criteria
- `/ready` stays non-200 until models loaded and warmed; logs show load metrics.

Validate
- pytest; manual curl.

Status Update
- make engineer-done ENGINEER=ml/engineer-01 TASK='ML-01.11' && make team-status-write

