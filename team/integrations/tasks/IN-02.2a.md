Task: IN-02.2a — Slot search mapping (paging, filters, TZ-safe windows)

Context
- Map GP Connect slot search responses into internal booking slot models, respecting paging and time zones.

Files
- packages/ports/src/gp-connect.ts
- schemas/booking/booking-search-request.json (reference)

Steps
1) Implement slot search call mapping to internal `Slot[]` with fields (start, end, type, location, providerRef, capacity).
2) Support paging/continuation where applicable; expose iterator or paging API to callers.
3) Apply filters (location, clinician, slot types) and normalize times to practice TZ, ensuring DST-safe comparisons.

Acceptance Criteria
- Deterministic mapping; TZ-safe; paging works; typecheck passes.

Validate
- Unit tests with fixtures covering DST boundaries, paging, and filtering combinations.

Status Update
- make engineer-done ENGINEER=integrations/engineer-02 TASK='IN-02.2a' && make team-status-write

