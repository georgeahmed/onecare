Task: IN-01.16 — Privacy & PHI minimization (payload shaping; audit-safe)

Context
- Ensure integrations never log PHI/PII and that payloads to audit contain only minimal, stable identifiers.

Files
- packages/observability/src/logger.ts (redaction)
- packages/ports/src/fhir.ts, object-store.ts
- docs/CONVENTIONS.md (data handling)

Steps
1) Extend redaction to mask tokens/IDs and collapse nested objects; only allow specific safe fields in logs (correlationId, operation, resourceType, ids).
2) When emitting audit intents, include only stable IDs (e.g., bundleId, taskId) and reason codes; never include narrative text or attachments.
3) Add unit tests ensuring log entries do not contain PHI and that audit payloads remain minimal.

Acceptance Criteria
- No PHI in logs/audit; tests pass.

Validate
- npm run test; inspect logs in a stubbed run.

Status Update
- make engineer-done ENGINEER=integrations/engineer-01 TASK='IN-01.16' && make team-status-write

