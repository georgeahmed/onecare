Task: IN-02.3a — Appointment create idempotency + conflict handling (natural key; exactly-once write)

Context
- Create appointments once, handle conflicts gracefully, and provide stable references for write-back.

Files
- packages/ports/src/gp-connect.ts
- packages/ports/src/fhir.ts (for write-back)

Steps
1) Define a natural idempotency key (patientId + slotId + practiceId + day) and propagate in headers or request body as supported.
2) On HTTP conflicts (409/409-equivalent): detect and map to `conflict` error with actionable details; retry safe when appropriate.
3) Ensure exactly-once semantics in orchestrated flow: return existing appointment reference on duplicate create.

Acceptance Criteria
- Duplicate create attempts do not produce multiple bookings; conflicts mapped clearly; stable references returned.

Validate
- Unit tests simulate duplicate/create conflict scenarios; verify idempotent behavior and mapping.

Status Update
- make engineer-done ENGINEER=integrations/engineer-02 TASK='IN-02.3a' && make team-status-write

