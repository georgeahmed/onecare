Navigation: [Task Index](../../../docs/TASK_INDEX.md) | [All Tasks Flow](../../all-tasks-flow.md) | [Prev](IN-01.10.md) | [Next](IN-01.12.md)

Task: IN-01.11 — FHIR search/pagination helpers (429 handling)

Context
- Provide convenient helpers for FHIR search with paging and 429 backoff handling using `Retry-After`.

Files
- packages/ports/src/fhir.ts
- packages/observability/src/logger.ts

Steps
1) Implement `search(resourceType, params)` returning an async iterator over pages (Bundle entries), handling `_getpages`/`next` links.
2) Respect `429 Too Many Requests` by honoring `Retry-After` header; apply jittered backoff.
3) Emit metrics for `fhir.search.pages` and `fhir.search.retry`.
4) Unit tests with fakes for next links and 429 responses.

Acceptance Criteria
- Async iterator yields items across pages; 429 handled; tests pass.

Validate
- npm run test

Status Update
- make engineer-done ENGINEER=integrations/engineer-01 TASK='IN-01.11' && make team-status-write

