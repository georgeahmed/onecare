Task: IN-01.12 — Observability (correlationId, metrics, spans; PHI-safe logs)

Context
- Ensure all outbound requests include `x-correlation-id`; add structured logs and metrics with PHI-safe redaction; wrap operations in spans when available.

Files
- packages/observability/src/logger.ts
- packages/observability/src/otel.ts
- packages/ports/src/fhir.ts, object-store.ts

Steps
1) Thread `correlationId` through all client calls (headers + logs). Add safe logging helpers that redact tokens/PII and collapse nested objects.
2) Metrics: request counts, latency histograms, error counters per destination; tag by operation.
3) Add simple spans for `fhir.transaction`, `fhir.create`, `fhir.search`, `object.put` with minimal, safe attributes.
4) Unit tests using fakes to assert log/metric calls include correlationId and exclude PHI.

Acceptance Criteria
- Logs/metrics/spans consistent and safe; tests pass.

Validate
- npm run test; inspect console output of a local stub run.

Status Update
- make engineer-done ENGINEER=integrations/engineer-01 TASK='IN-01.12' && make team-status-write

