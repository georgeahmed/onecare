Task: IN-03.17 — API version/content negotiation (CPCS/ICS headers; Accept/Content-Type)

Context
- Ensure correct protocol headers and content negotiation for CPCS and ICS APIs to avoid subtle compatibility issues.

Files
- apps/pharmacy-router/src/adapters/cpcs.client.ts
- apps/ics-hub/src/adapters/ics.client.ts

Steps
1) Set explicit `Accept` and `Content-Type` headers per provider spec (typically `application/json` or `application/fhir+json; charset=utf-8`).
2) Include provider-specific headers if required (trace/request ids) via config; do not hardcode values; never log secrets.
3) Validate response content-type; on mismatch, return a typed error for upstream mapping (no stack traces in bodies).
4) Unit tests asserting header sets and content-type validation for both CPCS and ICS clients.

Acceptance Criteria
- Headers/content negotiation correct; tests pass deterministically.

Validate
- npm run test

Status Update
- make engineer-done ENGINEER=integrations/engineer-03 TASK='IN-03.17' && make team-status-write

