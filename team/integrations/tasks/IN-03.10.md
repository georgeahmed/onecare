Task: IN-03.10 — DLQ and retry policy for ICS events

Context
- Ensure reliable ICS event publishing/consuming with bounded retries, jitter, and DLQ for poison messages.

Files
- apps/ics-hub/src/index.ts (publish/consume)
- packages/events/src/topics.ts (broker.dlq exists)

Steps
1) Apply max 3 retries with exponential backoff + jitter for publishes and consumes; annotate envelope meta with `attempt` and `firstSeenAt`.
2) On non-retryable error or retries exhausted, publish minimal DLQ record with topic, error code, correlationId, and safe context.
3) Honor idempotency keys on consume (reserve→handle→commit pattern) to prevent duplicate side effects.
4) Metrics: `event.retry`, `event.dlq`, `event.consume_error` tagged by topic and code.

Acceptance Criteria
- Retries bounded and jittered; DLQ receives poison messages; consumers dedupe.

Validate
- Unit tests force retries and DLQ; validate dedupe prevents duplicates.

Status Update
- make engineer-done ENGINEER=integrations/engineer-03 TASK='IN-03.10' && make team-status-write

