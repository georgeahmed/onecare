Navigation: [Task Index](../../../docs/TASK_INDEX.md) | [All Tasks Flow](../../all-tasks-flow.md) | [Prev](IN-01.19.md) | [Next](IN-01.3.md)

Task: IN-01.2 — Bundle upsert (transaction) with retry/timeout

Context
- Orchestrator needs atomic Bundle upsert to FHIR store with resilience, correlation, and validation.

Files
- packages/ports/src/fhir.ts

Steps
1) Implement upsertBundle(bundle) using FHIR $transaction endpoint (POST baseUrl) with `x-correlation-id` header propagated.
2) Apply timeout (configurable) and bounded retries with jitter on 5xx/timeouts; map non-2xx to typed errors for orchestrator mapping.
3) Validate response status and content-type; parse safely; return FhirBundle with id set (if available).
4) Emit metrics (latency histogram, error counters) and optional trace spans.

Acceptance Criteria
- Typecheck passes; method signature matches port; retries/timeouts applied; correlation propagated.

Validate
- npm run build && npm run typecheck

Status Update
- make engineer-done ENGINEER=integrations/engineer-01 TASK='IN-01.2' && make team-status-write
