Task: IN-03.11 — Health/readiness and graceful shutdown (Integrations)

Context
- Provide probes and orderly shutdown for adapters/clients to CPCS/ICS.

Files
- apps/pharmacy-router/src/index.ts (probes)
- apps/ics-hub/src/index.ts (probes)
- adapters (close/drain)

Steps
1) Add `/healthz` and `/readyz`; readiness reflects ability to reach CPCS/ICS or cached status.
2) On SIGTERM/SIGINT: stop accepting new requests, drain inflight work up to timeout (e.g., 10s), close clients, then exit.
3) Metrics: `shutdown.start`, `shutdown.timeout`, and gauge `inflight.requests`.

Acceptance Criteria
- Probes stable; readiness flips during startup/shutdown; shutdown within timeout.

Validate
- Manual: start service, send SIGTERM; observe logs/metrics; ensure clean exit.

Status Update
- make engineer-done ENGINEER=integrations/engineer-03 TASK='IN-03.11' && make team-status-write

