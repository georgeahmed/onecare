Task: IN-02.1c — Security: TLS verification, optional cert pinning, SSRF allowlist

Context
- Harden outbound connections to GP Connect endpoints.

Files
- packages/ports/src/gp-connect.ts
- docs/SECURITY.md (update)

Steps
1) Enforce TLS certificate verification; add optional certificate pinning (fingerprint) via config for high-assurance environments.
2) Introduce SSRF allowlist for hostnames; reject non-allowlisted targets.
3) Redact sensitive headers/fields in logs; ensure no PHI/PII is logged.

Acceptance Criteria
- Non-allowlisted hosts blocked; TLS verification enforced; redaction validated.

Validate
- Unit tests simulate blocked hosts; verify safe errors and logs.

Status Update
- make engineer-done ENGINEER=integrations/engineer-02 TASK='IN-02.1c' && make team-status-write

