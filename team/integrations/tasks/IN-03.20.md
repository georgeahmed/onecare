Task: IN-03.20 — Adapter-level rate limits/backpressure (per-org caps; 429/503 mapping)

Context
- Enforce fair usage and protect providers by applying per-organization rate limits and concurrency caps at the integration client level.

Files
- apps/ics-hub/src/adapters/ics.client.ts
- apps/pharmacy-router/src/adapters/cpcs.client.ts
- @onecare/config (per-org limits)

Steps
1) Implement token-bucket rate limiter per org/destination with configurable rates/bursts; expose metrics `ratelimit.hit/miss`.
2) Add concurrency semaphore to cap simultaneous calls per org; map saturation to 429/503 (typed errors for upstream envelopes) and set `Retry-After` where applicable.
3) Integrate with routing policy (IN-03.6) to read per-org settings.
4) Unit tests simulating bursts; assert caps and correct error mapping.

Acceptance Criteria
- Limits enforced; metrics/logs present; mapping consistent; tests pass.

Validate
- npm run test with synthetic burst scenarios.

Status Update
- make engineer-done ENGINEER=integrations/engineer-03 TASK='IN-03.20' && make team-status-write

