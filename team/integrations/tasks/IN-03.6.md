Task: IN-03.6 — ICS org→endpoint map and routing policy (allowlist; per-org limits)

Context
- Route ICS referrals via a config-driven org→endpoint map with allowlists and per-org limits to avoid abuse.

Files
- apps/ics-hub/src/application/ics.state.ts
- @onecare/config (mapping)

Steps
1) Define mapping structure: `{ orgCode: { endpoint, authRef, rateLimit } }` in config; load via `@onecare/config`.
2) Enforce allowlist: block unknown orgs with `forbidden` error; log safely.
3) Add per-org token-bucket limiter stub; on exceed return 429 with envelope in upstream handler.
4) Unit tests: allowed vs blocked org, limit exceed behavior.

Acceptance Criteria
- Mapping and policy enforced; tests pass; logs/metrics present.

Validate
- npm run test

Status Update
- make engineer-done ENGINEER=integrations/engineer-03 TASK='IN-03.6' && make team-status-write
