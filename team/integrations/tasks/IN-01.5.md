Task: IN-01.5 — OIDC (NHS Login) client + consent check stub API

Context
- Provide identity and consent checks per Algorithm.md 11) with deny-by-default semantics and correlation propagation.

Files
- packages/security/src/index.ts
- apps (later use)

Steps
1) Stub OIDC client config (discovery URL, client id/secret) (docs only or comments); include JWKs cache + signature verify plan.
2) Expose consent API shape: checkConsent(patientId, purpose, requestedResources) → allow/deny with reason; propagate `correlationId` in logs.
3) Deny-by-default if missing consent; redact tokens and PII in logs; document headers and scopes to enforce.

Acceptance Criteria
- SecurityServices interface aligned; stubs documented; deny-by-default documented; logs redacted.

Validate
- npm run typecheck

Status Update
- make engineer-done ENGINEER=integrations/engineer-01 TASK='IN-01.5' && make team-status-write
