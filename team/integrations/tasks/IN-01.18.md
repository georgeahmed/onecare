Task: IN-01.18 — Fault injection tests (timeouts, CB-open, 429/backoff)

Context
- Validate resilience with deterministic tests: timeouts, circuit-open recovery, and 429 Retry-After handling for FHIR search.

Files
- packages/ports/test/fhir.resilience.test.ts (new)
- packages/ports/src/fhir.ts (testability tweaks)

Steps
1) Add injectables/fakes for transport to simulate timeouts, 5xx, and 429 with Retry-After.
2) Assert backoff/retries invoke the handler the expected number of times and respect Retry-After.
3) Simulate CB-open, then half-open probe and recovery; validate metrics/logs.
4) Keep tests deterministic with fake timers; no real network.

Acceptance Criteria
- Tests cover timeout/retry/CB and 429 handling; deterministic and passing.

Validate
- npm run test

Status Update
- make engineer-done ENGINEER=integrations/engineer-01 TASK='IN-01.18' && make team-status-write

