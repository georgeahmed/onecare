Task: IN-01.10 — Idempotent FHIR operations (ETag/If-Match, 409/412 handling)

Context
- Ensure safe updates and retries by using FHIR conditional operations and HTTP preconditions. Handle conflicts deterministically.

Files
- packages/ports/src/fhir.ts
- docs/ERRORS.md

Steps
1) For resource updates, use ETag/If-Match when versioning is available, or conditional `PUT [type]?identifier=...` with stable identifiers; map 412/409 to typed errors.
2) For Bundle transaction, include conditional `ifNoneExist` where applicable to avoid duplicates.
3) On 409/412, surface a structured conflict error for higher layers (booking/pharmacy) to retry with fresh data or return 409 envelopes.
4) Add unit tests for 409/412 mapping and idempotent create/update semantics.

Acceptance Criteria
- Preconditions applied; conflicts mapped; tests cover behavior.

Validate
- npm run test

Status Update
- make engineer-done ENGINEER=integrations/engineer-01 TASK='IN-01.10' && make team-status-write

