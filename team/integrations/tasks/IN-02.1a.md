Task: IN-02.1a — GP Connect client foundation (HTTPS, mTLS/OAuth, env/config)

Context
- Provide a robust GP Connect HTTP client with proper authentication, configuration, and correlation propagation. Keep adapter logic clean and reusable by the Booking service.

Files
- apps (consumers later)
- packages/ports/src/gp-connect.ts (new interface)
- packages/config/src/contracts/orchestrator.ts (reference for config style)

Steps
1) Define `GpConnectClient` interface (searchSlots, createAppointment) with inputs/outputs aligned to booking contracts.
2) Implement base client with HTTPS agent and connection pooling; read endpoint/creds from env (`GPC_ENDPOINT`, `GPC_CLIENT_CERT`, `GPC_CLIENT_KEY`, `GPC_TOKEN`, etc.).
3) Support mTLS and/or OAuth/Bearer creds via pluggable auth provider; inject headers safely and redact in logs.
4) Propagate `x-correlation-id` on all requests; add minimal request/response logging (safe, redacted).

Acceptance Criteria
- Client compiles; env/config wiring complete; correlationId propagated; secrets never logged.

Validate
- Unit tests with http mocks (no network) covering header injection and redaction.

Status Update
- make engineer-done ENGINEER=integrations/engineer-02 TASK='IN-02.1a' && make team-status-write

