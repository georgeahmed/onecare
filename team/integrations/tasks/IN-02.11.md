Task: IN-02.11 — Privacy/PII minimization (PHI-safe logs; minimal event payloads)

Context
- Ensure GP Connect integration never logs PHI/PII and emits only minimal, stable identifiers in events/audits.

Files
- packages/observability/src/logger.ts (redaction helpers)
- apps/booking/src/adapters/gpconnect.client.ts
- docs/EVENTS.md (payload minimization)

Steps
1) Extend redaction to mask tokens/IDs and collapse nested objects. Only allow list safe fields in logs (correlationId, operation, endpoint host, practiceId, slotId, appointmentId).
2) Ensure emitted events/audits include only IDs, timestamps, and reason codes; never free-text or PHI-rich content.
3) Add unit tests verifying that logs and event payloads are PHI-safe.

Acceptance Criteria
- No PHI in logs or events; tests pass.

Validate
- npm run test; inspect structured logs with a local stub.

Status Update
- make engineer-done ENGINEER=integrations/engineer-02 TASK='IN-02.11' && make team-status-write

