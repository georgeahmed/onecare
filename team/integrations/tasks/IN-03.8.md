Task: IN-03.8 — Contract-first ICS mapping (schemas/codegen; compiled validators; contract tests)

Context
- Ensure ICS referral.request/ack payloads adhere to schemas. Validate at boundaries using compiled JSON Schemas and generated TS models.

Files
- schemas/ics/referral-request.json, schemas/ics/referral-ack.json
- packages/events/src/contracts/* (generated)
- apps/ics-hub/src/adapters/ics.client.ts

Steps
1) Verify ICS schemas are current; update `$id` and fields if needed (no PHI), default `additionalProperties: false`.
2) Run `npm run codegen` to regenerate TS models; import types in the client.
3) Compile validators and enforce on egress/ingress paths in ICS client/consumer.
4) Add contract tests: valid + invalid payloads; no network calls.

Acceptance Criteria
- Codegen clean; validators enforced; test suite covers happy/negative cases.

Validate
- npm run codegen && npm run typecheck && npm test

Status Update
- make engineer-done ENGINEER=integrations/engineer-03 TASK='IN-03.8' && make team-status-write

