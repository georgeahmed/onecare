Task: IN-03.1 — CPCS client interface skeleton (typed, env/config wiring)

Context
- Provide a typed CPCS client for referral creation (slot/slotless). Establish clean separation between interface (app) and HTTP adapter (infra), wire config/secrets, and prepare for resilience.

Files
- apps/pharmacy-router/src/adapters/cpcs.client.ts
- @onecare/config (CPCS endpoint/keys)

Steps
1) Define TypeScript interface: `sendReferral(orgId, referral: ReferralPayload, slot?: Slot): Promise<ReferralResult>` with minimal, typed payloads (no PHI in types used by logs).
2) Read CPCS_URL and credentials from config/env; do not log secrets. Support header injection via a small helper.
3) Return stubbed success/failure with stable error codes; prepare placeholders for timeout/retry/circuit breaker (to be implemented in IN-03.2).
4) Document required env vars in adapter comments; include a constructor that accepts overrides (for tests).

Acceptance Criteria
- Interface compiles; no secrets are logged; adapter constructor accepts config.

Validate
- npm run build && npm run typecheck

Status Update
- make engineer-done ENGINEER=integrations/engineer-03 TASK='IN-03.1' && make team-status-write
