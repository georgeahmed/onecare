Task: IN-02.5 — FHIR write-back links for created appointments (Task update; references; audit)

Context
- Ensure booking results are written back to FHIR and linked to originating Tasks, with audit events.

Files
- packages/ports/src/fhir.ts
- apps/booking

Steps
1) On successful booking, write Appointment to FHIR with references to Patient/Practitioner/Location as applicable.
2) Update originating Task (if present) with outcome and appointment reference; emit audit.event with correlationId.
3) Map errors to error taxonomy; never publish invalid/partial payloads.

Acceptance Criteria
- Appointment persisted; Task updated; audit event emitted; no partial writes on error.

Validate
- Unit tests stub FHIR repo; verify write-back + Task update behavior and error mapping.

Status Update
- make engineer-done ENGINEER=integrations/engineer-02 TASK='IN-02.5' && make team-status-write

