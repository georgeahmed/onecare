Task: IN-01.1 — FHIR client skeleton + env wiring (baseUrl, auth)

Context
- Provide a thin FHIR REST client used by ports implementations with timeouts, retries, metrics, and correlation.

Files
- packages/ports/src/fhir.ts (reference)
- apps (consumers later)

Steps
1) Create FHIR client class with options { baseUrl, authToken?, timeoutMs?, maxRetries? } and inject logger.
2) Expose request helper (GET/POST) with safe JSON parsing, timeout via AbortController, and bounded retries with jitter.
3) Read defaults from env: FHIR_BASE_URL, FHIR_TOKEN, FHIR_TIMEOUT_MS; propagate `x-correlation-id` header.
4) Emit metrics (latency histogram, error counters) and wrap calls in OTel spans if available.

Acceptance Criteria
- Build/typecheck passes; client compiles and can be imported; timeouts/retries/correlation implemented.

Validate
- npm run build && npm run typecheck

Status Update
- make engineer-done ENGINEER=integrations/engineer-01 TASK='IN-01.1' && make team-status-write
