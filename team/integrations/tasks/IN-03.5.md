Task: IN-03.5 — Observability for CPCS/ICS (structured logs, latency histograms, error counters, spans)

Context
- Provide consistent, PHI-safe observability for all integration calls.

Files
- apps/pharmacy-router/src/adapters/cpcs.client.ts
- apps/ics-hub/src/adapters/ics.client.ts
- packages/observability/src/logger.ts, src/otel.ts

Steps
1) Structured JSON logs with `correlationId`, `provider`, `endpoint`, `result` (codes only); redact tokens/PHI by default.
2) Emit metrics: counters for `integration.call.success/error/timeout/cb.open`; histograms for `integration.latency_ms` with labels provider= cpcs|ics.
3) Wrap calls in OTel spans (`cpcs.call`, `ics.call`) with attributes: `provider`, `endpoint`, `correlationId`, `result`.
4) Unit tests assert logger called with safe fields; no network I/O.

Acceptance Criteria
- Spans/metrics/logs consistent and PHI-safe; typecheck/tests pass.

Validate
- npm run build && npm run typecheck && npm test

Status Update
- make engineer-done ENGINEER=integrations/engineer-03 TASK='IN-03.5' && make team-status-write
