Task: IN-03.7 — Security & SSRF guardrails (CPCS/ICS)

Context
- Harden outbound HTTP to CPCS/ICS: allowlist endpoints, prevent private/loopback access, sanitize headers, and disable dangerous behaviors.

Files
- apps/pharmacy-router/src/adapters/cpcs.client.ts
- apps/ics-hub/src/adapters/ics.client.ts
- docs/CONVENTIONS.md (reference)

Steps
1) Implement URL allowlist per provider; resolve DNS and verify IP is public (no loopback/private/link-local). Disable automatic redirects or validate redirect targets.
2) Sanitize outbound headers to minimal required set; propagate `x-correlation-id` and `traceparent` only.
3) Block IP literals and IPv6 local addresses; return `upstream_blocked` error code.
4) Unit tests: disallow `127.0.0.1`/`::1`/RFC1918; allow configured host; validate headers.

Acceptance Criteria
- Outbound SSRF protections enforced; tests cover allow/deny cases; no sensitive headers leak.

Validate
- npm test

Status Update
- make engineer-done ENGINEER=integrations/engineer-03 TASK='IN-03.7' && make team-status-write

