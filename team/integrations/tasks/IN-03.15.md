Task: IN-03.15 — Credential management & rotation readiness

Context
- Handle credentials for CPCS/ICS safely via env/secret stores; ensure rotation doesn’t require rebuilds.

Files
- adapters: cpcs.client.ts, ics.client.ts
- README/ops notes

Steps
1) Read creds (API keys/JWT) from env or secret provider; never log; support runtime refresh (e.g., on timer or signal).
2) Document expected environment variables and rotation steps.
3) Add unit tests to assert secrets are never logged.

Acceptance Criteria
- Secrets only from env/secret store; rotation plan documented; tests pass.

Validate
- npm run typecheck && npm test

Status Update
- make engineer-done ENGINEER=integrations/engineer-03 TASK='IN-03.15' && make team-status-write

