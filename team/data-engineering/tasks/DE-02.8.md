Navigation: [Task Index](../../../docs/TASK_INDEX.md) | [All Tasks Flow](../../all-tasks-flow.md) | [Prev](DE-02.7.md) | [Next](DE-02.9.md)

Task: DE-02.8 — Online store adapter (memory/Redis) with TTL, upsert semantics, health/readiness

Context
- Provide a low-latency online feature store interface with TTL and upsert semantics for serving paths; include health/readiness checks.

Files
- packages/ports/src/feature-store.ts (reference)
- packages/feature-store-redis/* (new) or memory adapter

Steps
1) Implement `get(entityKey, featureNames[], asOfTs?)` and `put(entityKey, features, ttl)` with idempotent upserts and TTL expiration.
2) Add a health check (`ping` for Redis, internal status for memory) and readiness cached result.
3) Unit tests for TTL expiration, upsert, and concurrent access; no network in unit tests (use memory adapter).

Acceptance Criteria
- Adapter compiles; tests pass; health/readiness semantics defined.

Validate
- npm run build && npm run test

Status Update
- make engineer-done ENGINEER=data-engineering/engineer-02 TASK='DE-02.8' && make team-status-write

