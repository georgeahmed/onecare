Task: DE-01.12 — Incremental rollups (windowed p50/p95, idempotent upserts, backfill)

Context
- Compute incremental aggregates (counts, error rates, latency p50/p95) in sliding windows and upsert idempotently into rollup tables.

Files
- scripts/etl/rollup.ts or .py (new)

Steps
1) Define windows (e.g., 1m/5m/1h) and metrics to compute per metric name and label group.
2) Implement idempotent upsert keyed by (window_start, metric, labelHash) to avoid duplicate writes.
3) Provide a backfill mode to recompute over historical partitions, respecting idempotency.
4) Unit tests for windowing and upserts using synthetic data.

Acceptance Criteria
- Incremental rollups computed; upserts idempotent; backfill works; tests pass.

Validate
- Run rollup on a sample dataset and inspect outputs.

Status Update
- make engineer-done ENGINEER=data-engineering/engineer-01 TASK='DE-01.12' && make team-status-write

