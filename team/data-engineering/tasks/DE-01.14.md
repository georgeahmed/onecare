Task: DE-01.14 — Lineage & metadata (OpenLineage hooks; dataset/version tags)

Context
- Emit basic lineage and run metadata for ETL jobs to support traceability and governance.

Files
- scripts/etl/*
- docs/ANALYTICS_LINEAGE.md (new)

Steps
1) Add lightweight lineage emission (OpenLineage or JSON) capturing job name, inputs (topics/paths), outputs (tables/paths), and version tags.
2) Include dataset schema versions and code version (git hash) in run metadata; store alongside outputs.
3) Document conventions and how to visualize lineage (optional).

Acceptance Criteria
- Lineage metadata emitted with each run; includes versions; docs exist.

Validate
- Dry run to produce a lineage JSON; inspect recorded fields.

Status Update
- make engineer-done ENGINEER=data-engineering/engineer-01 TASK='DE-01.14' && make team-status-write

